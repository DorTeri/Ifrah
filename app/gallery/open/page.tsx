import React from 'react'

type Props = {}

const page = (props: Props) => {

    const getImagePath = (imageName: string) => {
        return `/assets/open/${imageName}.jpg`
    };
    return (
        <div>

        </div>
    )
}

export default page